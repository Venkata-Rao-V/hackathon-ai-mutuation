name: AI Mutation Testing CI

run-name: >-
  AI Mutation Testing (${{ github.event_name == 'workflow_dispatch' &&
  format('manual | branch={0} | scope={1}', inputs.target_branch, inputs.mutation_scope) ||
  github.ref_name }})

on:
  push:
    branches:
      - main
      - develop
      - feature/**
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:
    inputs:
      target_branch:
        description: "Branch to run"
        required: true
        type: choice
        default: main
        options:
          - main
          - develop
      mutation_scope:
        description: "Folder to scan for Python mutation targets"
        required: true
        type: string
        default: agent
      run_c_tests:
        description: "Run C compile & test job"
        required: false
        type: boolean
        default: true
      run_cpp_tests:
        description: "Run C++ compile & test job"
        required: false
        type: boolean
        default: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  PYTHON_VERSION: "3.11"
  SERVICE_URL: "http://127.0.0.1:8000"
  GRAFANA_URL: "http://127.0.0.1:3000"
  GRAFANA_USER: "admin"
  GRAFANA_PASSWORD: "admin"

permissions:
  contents: read

jobs:
  # ─────────────────────────────────────────────────────────────
  # JOB 1 — Lint (non-blocking, report only)
  # ─────────────────────────────────────────────────────────────
  lint:
    name: "1 · Lint (Non-Blocking)"
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Checkout repository (manual run)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_branch }}
          fetch-depth: 1

      - name: Checkout repository (push / PR)
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install flake8
        run: pip install flake8 flake8-bugbear flake8-html

      - name: Generate flake8 HTML report
        shell: bash
        run: |
          mkdir -p reports/flake8-html

          PY_FILES=$(find agent -type f -name '*.py' 2>/dev/null | sort | tr '\n' ' ')
          ROOT_PY=$(find . -maxdepth 1 -type f -name '*.py' 2>/dev/null | sort | tr '\n' ' ')

          if [ -z "$PY_FILES$ROOT_PY" ]; then
            python - <<'PY'
          from pathlib import Path
          Path("reports/flake8-html/index.html").write_text(
              """<!DOCTYPE html>
          <html>
          <head><meta charset="utf-8"><title>Flake8 Report</title></head>
          <body><h1>No Python files found to lint.</h1></body>
          </html>""",
              encoding="utf-8"
          )
          PY
            exit 0
          fi

          flake8 $PY_FILES $ROOT_PY \
            --max-line-length=120 \
            --extend-ignore=E203,W503,E501,W293,E261,E302,E305,E201,E202,F401,B007,E127 \
            --format=html \
            --htmldir=reports/flake8-html \
            --statistics || true

      - name: Upload flake8 report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flake8-html
          path: reports/flake8-html/
          if-no-files-found: warn

  # ─────────────────────────────────────────────────────────────
  # JOB 2 — Python Unit Tests
  # ─────────────────────────────────────────────────────────────
  unit_tests:
    name: "2 · Python Unit Tests"
    runs-on: ubuntu-latest
    needs: lint

    outputs:
      total: ${{ steps.metrics.outputs.total }}
      passed: ${{ steps.metrics.outputs.passed }}
      failed: ${{ steps.metrics.outputs.failed }}
      errors: ${{ steps.metrics.outputs.errors }}
      skipped: ${{ steps.metrics.outputs.skipped }}

    steps:
      - name: Checkout repository (manual run)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_branch }}
          fetch-depth: 1

      - name: Checkout repository (push / PR)
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-html junit2html

      - name: Create reports directory
        run: mkdir -p reports

      - name: Discover Python test files
        run: |
          find agent -type f \( -name 'test_*.py' -o -name '*_test.py' \) | sort > reports/pytest-files.txt
          echo "Discovered Python test files:"
          cat reports/pytest-files.txt || true

      - name: Run pytest and generate HTML report
        shell: bash
        run: |
          set +e

          if [ ! -s reports/pytest-files.txt ]; then
            echo "No Python test files found under agent/"
            python - <<'PY'
          from pathlib import Path
          Path("reports/pytest-report.html").write_text(
              """<!DOCTYPE html>
          <html>
          <head><meta charset="utf-8"><title>Pytest Report</title></head>
          <body><h1>No Python test files found under agent/</h1></body>
          </html>""",
              encoding="utf-8"
          )
          PY
            echo "0" > reports/pytest-exit-code.txt
            exit 0
          fi

          TEST_FILES=$(tr '\n' ' ' < reports/pytest-files.txt)
          python -m pytest $TEST_FILES \
            -v \
            --tb=short \
            --junitxml=reports/pytest-results.xml \
            --html=reports/pytest-report.html \
            --self-contained-html \
            --no-header

          echo "$?" > reports/pytest-exit-code.txt
          set -e

      - name: Convert JUnit XML to HTML
        run: |
          if [ -f reports/pytest-results.xml ]; then
            junit2html reports/pytest-results.xml reports/pytest-junit-report.html || true
          else
            python - <<'PY'
          from pathlib import Path
          Path("reports/pytest-junit-report.html").write_text(
              """<!DOCTYPE html>
          <html>
          <head><meta charset="utf-8"><title>JUnit Report</title></head>
          <body><h1>No JUnit XML generated.</h1></body>
          </html>""",
              encoding="utf-8"
          )
          PY
          fi

      - name: Parse pytest metrics
        id: metrics
        run: |
          python - <<'PY'
          import os
          import xml.etree.ElementTree as ET

          total = passed = failed = errors = skipped = 0

          xml_file = "reports/pytest-results.xml"
          if os.path.exists(xml_file):
              root = ET.parse(xml_file).getroot()
              suites = [root] if root.tag == "testsuite" else root.findall("testsuite")
              for suite in suites:
                  total   += int(suite.attrib.get("tests",    0))
                  failed  += int(suite.attrib.get("failures", 0))
                  errors  += int(suite.attrib.get("errors",   0))
                  skipped += int(suite.attrib.get("skipped",  0))

          passed = max(total - failed - errors - skipped, 0)

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as f:
              f.write(f"total={total}\n")
              f.write(f"passed={passed}\n")
              f.write(f"failed={failed}\n")
              f.write(f"errors={errors}\n")
              f.write(f"skipped={skipped}\n")
          PY

      - name: Upload pytest report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-html
          path: |
            reports/pytest-report.html
            reports/pytest-junit-report.html
          if-no-files-found: warn

# ─────────────────────────────────────────────────────────────
  # JOB 3A — C Compile & Test
  # ─────────────────────────────────────────────────────────────
  c_tests:
    name: "3A · C Compile & Test"
    runs-on: ubuntu-latest
    needs: lint
    continue-on-error: true
    if: ${{ always() && (github.event_name != 'workflow_dispatch' || inputs.run_c_tests) }}

    outputs:
      files: ${{ steps.metrics.outputs.files }}
      compile_pass: ${{ steps.metrics.outputs.compile_pass }}
      compile_fail: ${{ steps.metrics.outputs.compile_fail }}
      run_pass: ${{ steps.metrics.outputs.run_pass }}
      run_fail: ${{ steps.metrics.outputs.run_fail }}

    steps:
      - name: Checkout repository (manual run)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_branch }}
          fetch-depth: 1

      - name: Checkout repository (push / PR)
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install build tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y gcc make

      - name: Create reports directory
        run: mkdir -p reports

      - name: Discover C test files
        working-directory: agent
        run: |
          find . -type f -name 'test_*.c' | sort > ../reports/c-test-files.txt
          echo "Discovered C test files:"
          cat ../reports/c-test-files.txt || true

      - name: Compile and run C tests
        id: metrics
        working-directory: agent
        shell: bash
        run: |
          set +e
          LOG="../reports/c-build.log"
          : > "$LOG"

          FILES=0
          COMPILE_PASS=0
          COMPILE_FAIL=0
          RUN_PASS=0
          RUN_FAIL=0

          if [ ! -s ../reports/c-test-files.txt ]; then
            echo "No C test files discovered." | tee -a "$LOG"
          else
            while read -r TEST_FILE; do
              [ -z "$TEST_FILE" ] && continue
              FILES=$((FILES + 1))

              BASENAME=$(basename "$TEST_FILE")
              STEM="${BASENAME#test_}"
              STEM="${STEM%.c}"
              DIR=$(dirname "$TEST_FILE")

              SRC=""
              if [ -f "$DIR/${STEM}.c" ]; then
                SRC="$DIR/${STEM}.c"
              fi

              OUT="$DIR/${STEM}_c_test"

              echo "========================================" | tee -a "$LOG"
              echo "C TEST FILE : $TEST_FILE" | tee -a "$LOG"
              echo "C SOURCE    : ${SRC:-<none>}" | tee -a "$LOG"
              echo "OUTPUT      : $OUT" | tee -a "$LOG"

              if [ -n "$SRC" ]; then
                gcc -std=c11 -Wall -I. -o "$OUT" "$SRC" "$TEST_FILE" >> "$LOG" 2>&1
              else
                gcc -std=c11 -Wall -I. -o "$OUT" "$TEST_FILE" >> "$LOG" 2>&1
              fi

              if [ $? -eq 0 ]; then
                COMPILE_PASS=$((COMPILE_PASS + 1))
                echo "[BUILD PASS] $TEST_FILE" | tee -a "$LOG"
                "$OUT" >> "$LOG" 2>&1
                if [ $? -eq 0 ]; then
                  RUN_PASS=$((RUN_PASS + 1))
                  echo "[RUN PASS] $OUT" | tee -a "$LOG"
                else
                  RUN_FAIL=$((RUN_FAIL + 1))
                  echo "[RUN FAIL] $OUT" | tee -a "$LOG"
                fi
              else
                COMPILE_FAIL=$((COMPILE_FAIL + 1))
                echo "[BUILD FAIL] $TEST_FILE" | tee -a "$LOG"
              fi
            done < ../reports/c-test-files.txt
          fi

          {
            echo "files=$FILES"
            echo "compile_pass=$COMPILE_PASS"
            echo "compile_fail=$COMPILE_FAIL"
            echo "run_pass=$RUN_PASS"
            echo "run_fail=$RUN_FAIL"
          } >> "$GITHUB_OUTPUT"

      - name: Generate C HTML report
        run: |
          python - <<'PY'
          import html
          from pathlib import Path

          log_path = Path("reports/c-build.log")
          content = log_path.read_text(encoding="utf-8", errors="ignore") if log_path.exists() else "No C build log found."

          report = f"""<!DOCTYPE html>
          <html>
          <head>
            <meta charset="utf-8">
            <title>C Build Report</title>
            <style>
              body {{ font-family: Arial, sans-serif; margin: 24px; }}
              pre {{
                background: #111827;
                color: #f9fafb;
                padding: 16px;
                border-radius: 8px;
                white-space: pre-wrap;
              }}
            </style>
          </head>
          <body>
            <h1>C Build & Test Report</h1>
            <pre>{html.escape(content)}</pre>
          </body>
          </html>"""
          Path("reports/c-report.html").write_text(report, encoding="utf-8")
          PY

      - name: Upload C report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: c-html
          path: |
            reports/c-build.log
            reports/c-report.html
          if-no-files-found: warn

  # ─────────────────────────────────────────────────────────────
  # JOB 3B — C++ Compile & Test
  # ─────────────────────────────────────────────────────────────
  cpp_tests:
    name: "3B · C++ Compile & Test"
    runs-on: ubuntu-latest
    needs: lint
    continue-on-error: true
    if: ${{ always() && (github.event_name != 'workflow_dispatch' || inputs.run_cpp_tests) }}

    outputs:
      files: ${{ steps.metrics.outputs.files }}
      compile_pass: ${{ steps.metrics.outputs.compile_pass }}
      compile_fail: ${{ steps.metrics.outputs.compile_fail }}
      run_pass: ${{ steps.metrics.outputs.run_pass }}
      run_fail: ${{ steps.metrics.outputs.run_fail }}

    steps:
      - name: Checkout repository (manual run)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_branch }}
          fetch-depth: 1

      - name: Checkout repository (push / PR)
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install build tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y g++ make

      - name: Create reports directory
        run: mkdir -p reports

      - name: Discover C++ test files
        working-directory: agent
        run: |
          find . -type f \( -name 'test_*.cpp' -o -name 'test_*.cc' \) | sort > ../reports/cpp-test-files.txt
          echo "Discovered C++ test files:"
          cat ../reports/cpp-test-files.txt || true

      - name: Compile and run C++ tests
        id: metrics
        working-directory: agent
        shell: bash
        run: |
          set +e
          LOG="../reports/cpp-build.log"
          : > "$LOG"

          FILES=0
          COMPILE_PASS=0
          COMPILE_FAIL=0
          RUN_PASS=0
          RUN_FAIL=0

          if [ ! -s ../reports/cpp-test-files.txt ]; then
            echo "No C++ test files discovered." | tee -a "$LOG"
          else
            while read -r TEST_FILE; do
              [ -z "$TEST_FILE" ] && continue
              FILES=$((FILES + 1))

              BASENAME=$(basename "$TEST_FILE")
              STEM="${BASENAME#test_}"
              STEM="${STEM%.*}"
              EXT="${BASENAME##*.}"
              DIR=$(dirname "$TEST_FILE")

              SRC=""
              if [ -f "$DIR/${STEM}.${EXT}" ]; then
                SRC="$DIR/${STEM}.${EXT}"
              elif [ -f "$DIR/${STEM}.cpp" ]; then
                SRC="$DIR/${STEM}.cpp"
              elif [ -f "$DIR/${STEM}.cc" ]; then
                SRC="$DIR/${STEM}.cc"
              fi

              OUT="$DIR/${STEM}_cpp_test"

              echo "========================================" | tee -a "$LOG"
              echo "C++ TEST FILE: $TEST_FILE" | tee -a "$LOG"
              echo "C++ SOURCE   : ${SRC:-<none>}" | tee -a "$LOG"
              echo "OUTPUT       : $OUT" | tee -a "$LOG"

              if [ -n "$SRC" ]; then
                g++ -std=c++17 -Wall -I. -o "$OUT" "$SRC" "$TEST_FILE" >> "$LOG" 2>&1
              else
                g++ -std=c++17 -Wall -I. -o "$OUT" "$TEST_FILE" >> "$LOG" 2>&1
              fi

              if [ $? -eq 0 ]; then
                COMPILE_PASS=$((COMPILE_PASS + 1))
                echo "[BUILD PASS] $TEST_FILE" | tee -a "$LOG"
                "$OUT" >> "$LOG" 2>&1
                if [ $? -eq 0 ]; then
                  RUN_PASS=$((RUN_PASS + 1))
                  echo "[RUN PASS] $OUT" | tee -a "$LOG"
                else
                  RUN_FAIL=$((RUN_FAIL + 1))
                  echo "[RUN FAIL] $OUT" | tee -a "$LOG"
                fi
              else
                COMPILE_FAIL=$((COMPILE_FAIL + 1))
                echo "[BUILD FAIL] $TEST_FILE" | tee -a "$LOG"
              fi
            done < ../reports/cpp-test-files.txt
          fi

          {
            echo "files=$FILES"
            echo "compile_pass=$COMPILE_PASS"
            echo "compile_fail=$COMPILE_FAIL"
            echo "run_pass=$RUN_PASS"
            echo "run_fail=$RUN_FAIL"
          } >> "$GITHUB_OUTPUT"

      - name: Generate C++ HTML report
        run: |
          python - <<'PY'
          import html
          from pathlib import Path

          log_path = Path("reports/cpp-build.log")
          content = log_path.read_text(encoding="utf-8", errors="ignore") if log_path.exists() else "No C++ build log found."

          report = f"""<!DOCTYPE html>
          <html>
          <head>
            <meta charset="utf-8">
            <title>C++ Build Report</title>
            <style>
              body {{ font-family: Arial, sans-serif; margin: 24px; }}
              pre {{
                background: #111827;
                color: #f9fafb;
                padding: 16px;
                border-radius: 8px;
                white-space: pre-wrap;
              }}
            </style>
          </head>
          <body>
            <h1>C++ Build & Test Report</h1>
            <pre>{html.escape(content)}</pre>
          </body>
          </html>"""
          Path("reports/cpp-report.html").write_text(report, encoding="utf-8")
          PY

      - name: Upload C++ report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cpp-html
          path: |
            reports/cpp-build.log
            reports/cpp-report.html
          if-no-files-found: warn

  # # ─────────────────────────────────────────────────────────────
  # # JOB 4 — Python Mutation Testing (mutmut)
  # # ─────────────────────────────────────────────────────────────
  # mutation_tests:
  #   name: "4 · Mutation Testing"
  #   runs-on: ubuntu-latest
  #   needs: unit_tests
  #   continue-on-error: true

  #   outputs:
  #     killed: ${{ steps.metrics.outputs.killed }}
  #     survived: ${{ steps.metrics.outputs.survived }}
  #     timeout: ${{ steps.metrics.outputs.timeout }}
  #     total_mutants: ${{ steps.metrics.outputs.total_mutants }}
  #     mutation_score: ${{ steps.metrics.outputs.mutation_score }}

  #   steps:
  #     - name: Checkout repository (manual run)
  #       if: ${{ github.event_name == 'workflow_dispatch' }}
  #       uses: actions/checkout@v4
  #       with:
  #         ref: ${{ inputs.target_branch }}
  #         fetch-depth: 1

  #     - name: Checkout repository (push / PR)
  #       if: ${{ github.event_name != 'workflow_dispatch' }}
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 1

  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: ${{ env.PYTHON_VERSION }}
  #         cache: pip

  #     - name: Install dependencies
  #       run: |
  #         pip install -r requirements.txt
  #         pip install mutmut pytest

  #     - name: Determine mutation scope
  #       id: scope
  #       run: |
  #         SCOPE="${{ github.event_name == 'workflow_dispatch' && inputs.mutation_scope || 'agent' }}"
  #         echo "scope=$SCOPE" >> "$GITHUB_OUTPUT"
  #         echo "Mutation scope: $SCOPE"

  #     - name: Run mutmut
  #       id: mutmut_run
  #       shell: bash
  #       run: |
  #         set +e
  #         mkdir -p reports

  #         SCOPE="${{ steps.scope.outputs.scope }}"

  #         # Collect source files to mutate (non-test Python files in scope folder)
  #         SRC_FILES=$(find "$SCOPE" -type f -name '*.py' \
  #           ! -name 'test_*.py' ! -name '*_test.py' \
  #           2>/dev/null | sort | tr '\n' ' ')

  #         if [ -z "$SRC_FILES" ]; then
  #           echo "No Python source files found in $SCOPE to mutate."
  #           echo "0 0 0 0" > reports/mutmut-summary.txt
  #           exit 0
  #         fi

  #         # Build a pytest paths string targeting tests inside the scope folder
  #         TEST_FILES=$(find "$SCOPE" -type f \( -name 'test_*.py' -o -name '*_test.py' \) \
  #           2>/dev/null | sort | tr '\n' ' ')
  #         TEST_ARG=""
  #         [ -n "$TEST_FILES" ] && TEST_ARG="--test-command=\"python -m pytest $TEST_FILES -x -q\""

  #         # Run mutmut on the discovered source files
  #         eval mutmut run \
  #           --paths-to-mutate "$SCOPE" \
  #           --use-coverage \
  #           $TEST_ARG \
  #           2>&1 | tee reports/mutmut-run.log || true

  #         # Export results to JSON
  #         mutmut junitxml > reports/mutmut-results.xml 2>/dev/null || true
  #         mutmut show 2>/dev/null | tee reports/mutmut-show.log || true

  #         # Extract summary counts from mutmut results
  #         KILLED=$(mutmut results 2>/dev/null | grep -c "Killed" || echo 0)
  #         SURVIVED=$(mutmut results 2>/dev/null | grep -c "Survived" || echo 0)
  #         TIMEOUT=$(mutmut results 2>/dev/null | grep -c "Timeout" || echo 0)

  #         echo "$KILLED $SURVIVED $TIMEOUT" > reports/mutmut-summary.txt
  #         set -e

  #     - name: Parse mutation metrics
  #       id: metrics
  #       run: |
  #         python - <<'PY'
  #         import os, re
  #         from pathlib import Path

  #         killed = survived = timeout = 0

  #         summary = Path("reports/mutmut-summary.txt")
  #         if summary.exists():
  #             parts = summary.read_text().split()
  #             if len(parts) >= 3:
  #                 killed, survived, timeout = int(parts[0]), int(parts[1]), int(parts[2])

  #         # Also try to parse from the run log for richer data
  #         run_log = Path("reports/mutmut-run.log")
  #         if run_log.exists():
  #             text = run_log.read_text()
  #             for pattern, var in [
  #                 (r"(\d+) killed", "killed"),
  #                 (r"(\d+) survived", "survived"),
  #                 (r"(\d+) timed out", "timeout"),
  #             ]:
  #                 m = re.search(pattern, text, re.IGNORECASE)
  #                 if m:
  #                     if var == "killed":   killed   = int(m.group(1))
  #                     if var == "survived": survived = int(m.group(1))
  #                     if var == "timeout":  timeout  = int(m.group(1))

  #         total = killed + survived + timeout
  #         score = round((killed / total) * 100, 2) if total > 0 else 0.0

  #         with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as f:
  #             f.write(f"killed={killed}\n")
  #             f.write(f"survived={survived}\n")
  #             f.write(f"timeout={timeout}\n")
  #             f.write(f"total_mutants={total}\n")
  #             f.write(f"mutation_score={score}\n")

  #         print(f"Mutants — killed={killed}, survived={survived}, timeout={timeout}, score={score}%")
  #         PY

  #     - name: Generate mutation HTML report
  #       run: |
  #         python - <<'PY'
  #         import html
  #         from pathlib import Path

  #         run_log  = Path("reports/mutmut-run.log")
  #         show_log = Path("reports/mutmut-show.log")

  #         run_text  = run_log.read_text(encoding="utf-8", errors="ignore")  if run_log.exists()  else "No mutmut run log."
  #         show_text = show_log.read_text(encoding="utf-8", errors="ignore") if show_log.exists() else "No surviving mutants to show."

  #         report = f"""<!DOCTYPE html>
  #         <html><head><meta charset="utf-8"><title>Mutation Testing Report</title>
  #         <style>
  #           body{{font-family:Arial,sans-serif;margin:24px}}
  #           pre{{background:#111827;color:#f9fafb;padding:16px;border-radius:8px;white-space:pre-wrap;overflow-x:auto}}
  #           h2{{margin-top:28px}}
  #         </style></head><body>
  #         <h1>Mutation Testing Report</h1>
  #         <h2>Run log</h2><pre>{html.escape(run_text)}</pre>
  #         <h2>Surviving mutants</h2><pre>{html.escape(show_text)}</pre>
  #         </body></html>"""

  #         Path("reports/mutation-report.html").write_text(report, encoding="utf-8")
  #         PY

  #     - name: Upload mutation report
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: mutation-html
  #         path: |
  #           reports/mutation-report.html
  #           reports/mutmut-results.xml
  #           reports/mutmut-run.log
  #           reports/mutmut-show.log
  #         if-no-files-found: warn

  # # ─────────────────────────────────────────────────────────────
  # # JOB 5 — Push metrics to Grafana dashboard
  # # ─────────────────────────────────────────────────────────────
  # grafana_push:
  #   name: "5 · Push Metrics to Grafana"
  #   runs-on: ubuntu-latest
  #   if: always()
  #   needs:
  #     - unit_tests
  #     - mutation_tests
  #     - c_tests
  #     - cpp_tests

  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 1

  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: ${{ env.PYTHON_VERSION }}

  #     - name: Install requests
  #       run: pip install requests

  #     - name: Push metrics to Grafana
  #       env:
  #         # Unit test outputs
  #         UT_TOTAL: ${{ needs.unit_tests.outputs.total || '0' }}
  #         UT_PASSED: ${{ needs.unit_tests.outputs.passed || '0' }}
  #         UT_FAILED: ${{ needs.unit_tests.outputs.failed || '0' }}
  #         UT_ERRORS: ${{ needs.unit_tests.outputs.errors || '0' }}
  #         UT_SKIPPED: ${{ needs.unit_tests.outputs.skipped || '0' }}
  #         # Mutation test outputs
  #         MUT_KILLED: ${{ needs.mutation_tests.outputs.killed || '0' }}
  #         MUT_SURVIVED: ${{ needs.mutation_tests.outputs.survived || '0' }}
  #         MUT_TIMEOUT: ${{ needs.mutation_tests.outputs.timeout || '0' }}
  #         MUT_TOTAL: ${{ needs.mutation_tests.outputs.total_mutants || '0' }}
  #         MUT_SCORE: ${{ needs.mutation_tests.outputs.mutation_score || '0' }}
  #         # C test outputs
  #         C_FILES: ${{ needs.c_tests.outputs.files || '0' }}
  #         C_COMPILE_PASS: ${{ needs.c_tests.outputs.compile_pass || '0' }}
  #         C_COMPILE_FAIL: ${{ needs.c_tests.outputs.compile_fail || '0' }}
  #         C_RUN_PASS: ${{ needs.c_tests.outputs.run_pass || '0' }}
  #         C_RUN_FAIL: ${{ needs.c_tests.outputs.run_fail || '0' }}
  #         # C++ test outputs
  #         CPP_FILES: ${{ needs.cpp_tests.outputs.files || '0' }}
  #         CPP_COMPILE_PASS: ${{ needs.cpp_tests.outputs.compile_pass || '0' }}
  #         CPP_COMPILE_FAIL: ${{ needs.cpp_tests.outputs.compile_fail || '0' }}
  #         CPP_RUN_PASS: ${{ needs.cpp_tests.outputs.run_pass || '0' }}
  #         CPP_RUN_FAIL: ${{ needs.cpp_tests.outputs.run_fail || '0' }}
  #         # Grafana connection
  #         GRAFANA_URL: ${{ env.GRAFANA_URL }}
  #         GRAFANA_USER: ${{ env.GRAFANA_USER }}
  #         GRAFANA_PASSWORD: ${{ env.GRAFANA_PASSWORD }}
  #         # GitHub context
  #         GH_REPO: ${{ github.repository }}
  #         GH_BRANCH: ${{ github.ref_name }}
  #         GH_SHA: ${{ github.sha }}
  #         GH_RUN_ID: ${{ github.run_id }}
  #       run: |
  #         python - <<'PY'
  #         import os, time, requests, json

  #         grafana_url  = os.environ["GRAFANA_URL"].rstrip("/")
  #         grafana_user = os.environ["GRAFANA_USER"]
  #         grafana_pass = os.environ["GRAFANA_PASSWORD"]
  #         auth = (grafana_user, grafana_pass)

  #         repo   = os.environ["GH_REPO"]
  #         branch = os.environ["GH_BRANCH"]
  #         sha    = os.environ["GH_SHA"]
  #         run_id = os.environ["GH_RUN_ID"]

  #         now_ms = int(time.time() * 1000)  # Grafana expects epoch milliseconds

  #         # ── Helper: post to Grafana Annotations (marks the run on all dashboards) ──
  #         def annotate(text: str, tags: list[str]):
  #             payload = {
  #                 "time": now_ms,
  #                 "tags": tags,
  #                 "text": text,
  #             }
  #             r = requests.post(
  #                 f"{grafana_url}/api/annotations",
  #                 auth=auth,
  #                 json=payload,
  #                 timeout=10,
  #             )
  #             print(f"[annotation] {r.status_code} — {r.text[:120]}")

  #         # ── Helper: push via Grafana HTTP API (datasource "SimpleJSON" or "Infinity") ──
  #         # We use the /api/tsdb/query approach with a JSON body that stores to a
  #         # simple JSON datasource, OR we post to a webhook / push gateway.
  #         # For maximum compatibility we push to Grafana's built-in
  #         # "annotations" endpoint and also store a structured JSON payload
  #         # to the Grafana "Public API" dashboard variable via /api/dashboards.
  #         # Most self-hosted Grafana setups accept Prometheus push via push-gateway
  #         # or directly via the built-in "Loki" / "InfluxDB line protocol" endpoint.
  #         # Here we use the Grafana HTTP API "simple annotations" path which works
  #         # out of the box with no extra datasource configuration.

  #         metrics = {
  #             # unit tests
  #             "unit_total":   int(os.environ["UT_TOTAL"]),
  #             "unit_passed":  int(os.environ["UT_PASSED"]),
  #             "unit_failed":  int(os.environ["UT_FAILED"]),
  #             "unit_errors":  int(os.environ["UT_ERRORS"]),
  #             "unit_skipped": int(os.environ["UT_SKIPPED"]),
  #             # mutation
  #             "mut_killed":   int(os.environ["MUT_KILLED"]),
  #             "mut_survived": int(os.environ["MUT_SURVIVED"]),
  #             "mut_timeout":  int(os.environ["MUT_TIMEOUT"]),
  #             "mut_total":    int(os.environ["MUT_TOTAL"]),
  #             "mut_score":    float(os.environ["MUT_SCORE"]),
  #             # C
  #             "c_files":        int(os.environ["C_FILES"]),
  #             "c_compile_pass": int(os.environ["C_COMPILE_PASS"]),
  #             "c_compile_fail": int(os.environ["C_COMPILE_FAIL"]),
  #             "c_run_pass":     int(os.environ["C_RUN_PASS"]),
  #             "c_run_fail":     int(os.environ["C_RUN_FAIL"]),
  #             # C++
  #             "cpp_files":        int(os.environ["CPP_FILES"]),
  #             "cpp_compile_pass": int(os.environ["CPP_COMPILE_PASS"]),
  #             "cpp_compile_fail": int(os.environ["CPP_COMPILE_FAIL"]),
  #             "cpp_run_pass":     int(os.environ["CPP_RUN_PASS"]),
  #             "cpp_run_fail":     int(os.environ["CPP_RUN_FAIL"]),
  #         }

  #         # ── 1. Post a rich annotation so results appear on every dashboard panel ──
  #         summary_lines = [
  #             f"<b>CI Run</b> · {repo} @ {branch} ({sha[:8]}) · run {run_id}",
  #             f"Unit tests: {metrics['unit_passed']}/{metrics['unit_total']} passed",
  #             f"Mutation score: {metrics['mut_score']}%  "
  #             f"(killed {metrics['mut_killed']}, survived {metrics['mut_survived']})",
  #             f"C: {metrics['c_run_pass']} pass / {metrics['c_run_fail']} fail",
  #             f"C++: {metrics['cpp_run_pass']} pass / {metrics['cpp_run_fail']} fail",
  #         ]
  #         annotate(
  #             text="\n".join(summary_lines),
  #             tags=["ci", "mutation", branch, repo.replace("/", "-")],
  #         )

  #         # ── 2. Push individual metrics as separate annotations (panel tag filters) ──
  #         metric_annotations = [
  #             ("unit_passed",   metrics["unit_passed"],   "unit-tests"),
  #             ("unit_failed",   metrics["unit_failed"],   "unit-tests"),
  #             ("mut_score",     metrics["mut_score"],     "mutation"),
  #             ("mut_killed",    metrics["mut_killed"],    "mutation"),
  #             ("mut_survived",  metrics["mut_survived"],  "mutation"),
  #             ("c_run_pass",    metrics["c_run_pass"],    "c-tests"),
  #             ("c_run_fail",    metrics["c_run_fail"],    "c-tests"),
  #             ("cpp_run_pass",  metrics["cpp_run_pass"],  "cpp-tests"),
  #             ("cpp_run_fail",  metrics["cpp_run_fail"],  "cpp-tests"),
  #         ]

  #         for name, value, tag in metric_annotations:
  #             annotate(
  #                 text=f"{name}={value}",
  #                 tags=["ci-metric", tag, branch],
  #             )

  #         # ── 3. Optionally push to Prometheus push-gateway if PUSHGATEWAY_URL is set ──
  #         # Uncomment and set PUSHGATEWAY_URL secret to enable.
  #         pushgateway = os.environ.get("PUSHGATEWAY_URL", "")
  #         if pushgateway:
  #             lines = []
  #             job_label = f'job="ci",repo="{repo}",branch="{branch}"'
  #             for key, val in metrics.items():
  #                 metric_name = f"ci_{key}"
  #                 lines.append(f"# TYPE {metric_name} gauge")
  #                 lines.append(f'{metric_name}{{{job_label}}} {val}')
  #             payload = "\n".join(lines) + "\n"
  #             r = requests.post(
  #                 f"{pushgateway.rstrip('/')}/metrics/job/ci_pipeline",
  #                 data=payload,
  #                 headers={"Content-Type": "text/plain"},
  #                 timeout=10,
  #             )
  #             print(f"[pushgateway] {r.status_code} — {r.text[:120]}")

  #         print("Grafana push complete.")
  #         print(json.dumps(metrics, indent=2))
  #         PY
